'use client'

import Link from 'next/link'
import { useEffect, useMemo, useRef, useState } from 'react'
import { ArrowDownLeft, ArrowUpRight, BarChart3, FileSpreadsheet, Pencil, PiggyBank, Save, Upload, Wallet, X } from 'lucide-react'
import * as XLSX from 'xlsx'
import { MESES_2026 } from '../../lib/modelo-orcamento'

const meses = [...MESES_2026]
const STORAGE_KEY = 'orcamento-familiar-homologacao-2026'
type Dados = Record<string, number[]>
const moeda = (v:number) => v===0?'—':v.toLocaleString('pt-BR',{style:'currency',currency:'BRL',minimumFractionDigits:2,maximumFractionDigits:2})
const nav=[{href:'/controle',label:'Dashboard',icon:BarChart3},{href:'/lancamentos',label:'Lançamentos',icon:ArrowDownLeft},{href:'/contas',label:'Contas',icon:Wallet},{href:'/investimentos',label:'Investimentos',icon:PiggyBank},{href:'/homologacao',label:'Homologação',icon:FileSpreadsheet}]
function normalizarValor(raw:unknown){if(typeof raw==='number')return Number.isFinite(raw)?raw:0;let t=String(raw??'').trim().replace(/R\$\s?/gi,'');if(!t)return 0;if(t.includes(','))t=t.replace(/\./g,'').replace(',','.');const n=Number(t);return Number.isFinite(n)?n:0}
function parseRows(rows:unknown[][]):Dados{const result:Dados={};const header=(rows[0]||[]).map(v=>String(v??'').trim());const indices=meses.map(m=>header.findIndex(h=>h===m));rows.slice(1).forEach(row=>{const label=String(row?.[0]??'').trim();if(!label||label==='Categoria')return;result[label]=indices.map(i=>i<0?0:normalizarValor(row?.[i]))});return result}
function parseCsv(text:string):Dados{const rows=text.replace(/^\uFEFF/,'').trim().split(/\r?\n/).map(line=>{const d=line.includes(';')?';':',';return line.split(d).map(v=>v.trim().replace(/^\"|\"$/g,''))});return parseRows(rows)}
async function parseArquivo(file:File):Promise<Dados>{const ext=file.name.toLowerCase().split('.').pop();if(ext==='xlsx'||ext==='xls'){const buffer=await file.arrayBuffer();const wb=XLSX.read(buffer,{type:'array',cellDates:false});const sheet=wb.Sheets[wb.SheetNames[0]];if(!sheet)throw new Error('Planilha sem aba válida');return parseRows(XLSX.utils.sheet_to_json(sheet,{header:1,defval:''}) as unknown[][])}if(ext==='csv')return parseCsv(await file.text());throw new Error('Formato não suportado')}
function BarChart({receita,despesas}:{receita:number[];despesas:number[]}){const max=Math.max(...receita,...despesas,1);return <div className="chartBox"><div className="barChart">{meses.map((m,i)=>{const rh=Math.max(3,receita[i]/max*100),dh=Math.max(3,despesas[i]/max*100);return <div className="barGroup" key={m}><div className="bars"><i className="barIncome" style={{height:`${rh}%`}}/><i className="barExpense" style={{height:`${dh}%`}}/></div><span>{m.split('/')[0]}</span></div>})}</div><div className="chartLegend"><span><i className="legendIncome"/>Receita</span><span><i className="legendExpense"/>Despesas</span></div></div>}
function Donut({values,total,labels}:{values:number[];total:number;labels:string[]}){const safe=values.map(v=>Math.max(0,v));const sum=safe.reduce((a,b)=>a+b,0)||1;let start=0;const colors=['#0f766e','#2b6f85','#65a9ad','#8a9aa0','#d5a14a'];const parts=safe.map((v,i)=>{const end=start+v/sum*100;const p={label:labels[i],value:v,start,end,color:colors[i]};start=end;return p});const gradient=parts.map(p=>`${p.color} ${p.start}% ${p.end}%`).join(', ');return <div className="donutWrap"><div className="donut" style={{background:`conic-gradient(${gradient})`}}><div className="donutHole"><strong>{moeda(total)}</strong><span>Total</span></div></div><div className="donutLegend">{parts.map(p=><div key={p.label}><span><i style={{background:p.color}}/>{p.label}</span><b>{(sum?p.value/sum*100:0).toFixed(0)}%</b></div>)}</div></div>}
function LineChart({values}:{values:number[]}){const width=640,height=180,pad=22,max=Math.max(...values,1),min=Math.min(...values,0),range=Math.max(max-min,1);const points=values.map((v,i)=>{const x=pad+i*(width-pad*2)/(values.length-1);const y=height-pad-(v-min)/range*(height-pad*2);return `${x},${y}`}).join(' ');return <div className="lineChartWrap"><svg viewBox={`0 0 ${width} ${height}`} className="lineChart" role="img" aria-label="Evolução do fluxo de caixa do período"><line x1={pad} y1={height-pad} x2={width-pad} y2={height-pad} className="axis"/><polyline points={`${pad},${height-pad} ${points} ${width-pad},${height-pad}`} className="areaLine"/><polyline points={points} className="mainLine"/>{values.map((v,i)=>{const x=pad+i*(width-pad*2)/(values.length-1);const y=height-pad-(v-min)/range*(height-pad*2);return <circle key={i} cx={x} cy={y} r="3.5" className="linePoint"/>})}</svg><div className="lineLabels">{meses.map(m=><span key={m}>{m.split('/')[0]}</span>)}</div></div>}
export default function ControlePage(){const[dados,setDados]=useState<Dados>({});const[importado,setImportado]=useState(false);const[editando,setEditando]=useState<string|null>(null);const[rascunho,setRascunho]=useState<number[]>([]);const inputRef=useRef<HTMLInputElement>(null);useEffect(()=>{try{const salvo=localStorage.getItem(STORAGE_KEY);if(salvo){setDados(JSON.parse(salvo));setImportado(true)}}catch{}} ,[]);async function importar(file?:File){if(!file)return;try{const parsed=await parseArquivo(file);if(!Object.keys(parsed).length)throw new Error('Nenhum dado encontrado');setDados(parsed);localStorage.setItem(STORAGE_KEY,JSON.stringify(parsed));setImportado(true);setEditando(null)}catch{alert('Não foi possível importar. Selecione o modelo Excel (.xlsx/.xls) ou CSV com Jan/26 até Dez/26.')}finally{if(inputRef.current)inputRef.current.value=''}}const valor=(linha:string,i:number)=>Number(dados[linha]?.[i]||0);const receitaRows=[{label:'Salários',key:'Salários'},{label:'Férias',key:'Férias'},{label:'13º Salário',key:'13º Salário'},{label:'Bônus',key:'Bônus'},{label:'IR / Dissídio',key:'IR / Dissídio'}];const despesaRows=[{label:'Despesas Fixas',key:'Despesas Fixas'},{label:'Bancos e Acordos',key:'Bancos e Acordos'}];const fluxoRows=[{label:'Fluxo de Caixa',key:'Fluxo de caixa'},{label:'Fluxo de Caixa do Período',key:'Fluxo de Caixa do Período'}];const receitaMensal=meses.map((_,i)=>receitaRows.reduce((s,r)=>s+valor(r.key,i),0));const despesasMensal=meses.map((_,i)=>despesaRows.reduce((s,r)=>s+valor(r.key,i),0));const fluxoMensal=meses.map((_,i)=>valor('Fluxo de Caixa do Período',i));const anual=useMemo(()=>({renda:receitaMensal.reduce((s,v)=>s+v,0),despesas:despesasMensal.reduce((s,v)=>s+v,0),fluxo:fluxoMensal.reduce((s,v)=>s+v,0)}),[dados]);const taxaPoupanca=anual.renda>0?anual.fluxo/anual.renda*100:0;function iniciarEdicao(key:string){setEditando(key);setRascunho(meses.map((_,i)=>valor(key,i)))}function alterarRascunho(index:number,raw:string){setRascunho(prev=>{const next=[...prev];next[index]=normalizarValor(raw);return next})}function salvarEdicao(){if(!editando)return;const next={...dados,[editando]:rascunho};setDados(next);localStorage.setItem(STORAGE_KEY,JSON.stringify(next));setEditando(null);setRascunho([])}function cancelarEdicao(){setEditando(null);setRascunho([])}function celulas(key:string,destaque=false){return meses.map((m,i)=><td key={`${key}-${m}`} className={`border-t ${destaque?'border-t-2 border-slate-300 bg-slate-50':'border-slate-200'} px-3 py-3 text-right tabular-nums`}>{editando===key?<input aria-label={`${key} ${m}`} type="number" step="0.01" value={rascunho[i]??0} onChange={e=>alterarRascunho(i,e.target.value)} className="w-[98px] rounded-md border border-slate-300 bg-white px-2 py-1 text-right text-xs outline-none focus:border-teal-600"/>:moeda(valor(key,i))}</td>)}function linhaEditavel(label:string,key:string,destaque=false){return <tr key={key}><td className={`sticky left-0 z-10 border-r ${destaque?'border-t-2 border-slate-300 bg-slate-50':'border-t border-slate-200 bg-white'} px-4 py-3 ${destaque?'font-bold text-slate-800':'pl-8 text-slate-600'}`}><div className="flex min-w-[250px] items-center justify-between gap-2"><span>{label}</span>{editando===key?<span className="flex gap-1"><button type="button" title="Salvar" aria-label={`Salvar ${label}`} onClick={salvarEdicao} style={{display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'5px',border:0,borderRadius:6,background:'#0f766e',color:'#fff',cursor:'pointer'}}><Save size={13}/></button><button type="button" title="Cancelar" aria-label={`Cancelar ${label}`} onClick={cancelarEdicao} style={{display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'5px',border:'1px solid #d8e1e4',borderRadius:6,background:'#fff',color:'#60757d',cursor:'pointer'}}><X size={13}/></button></span>:<button type="button" title={`Editar ${label}`} aria-label={`Editar ${label}`} onClick={()=>iniciarEdicao(key)} style={{display:'inline-flex',alignItems:'center',justifyContent:'center',gap:4,padding:'5px 7px',border:'1px solid #cfe0df',borderRadius:6,background:'#f2f8f7',color:'#0f766e',cursor:'pointer',fontSize:10}}><Pencil size={12}/>Editar</button>}</div></td>{celulas(key,destaque)}</tr>}
return <div className="app"><aside className="sidebar"><div className="brand"><div className="brandMark">R$</div><div><strong>Orçamento</strong><span>Familiar</span></div></div><nav>{nav.map(n=>{const Icon=n.icon;return <Link key={n.href} href={n.href} className={n.href==='/controle'?'active':''}><Icon size={17}/>{n.label}</Link>})}</nav><div className="sideBottom"><small>VISÃO EXECUTIVA</small><p>Indicadores financeiros da família em uma visão simples e objetiva.</p></div></aside><main className="content"><header className="top"><div><p className="eyebrow">ORÇAMENTO FAMILIAR</p><h1>Dashboard financeiro</h1><p className="muted">Visão sintética da situação financeira · 2026</p></div><div className="topActions"><button className="statusPill" onClick={()=>inputRef.current?.click()}><Upload size={14}/>{importado?'Atualizar base':'Carregar base'}</button><input ref={inputRef} type="file" accept=".xlsx,.xls,.csv" className="hidden" onChange={e=>importar(e.target.files?.[0])}/></div></header>{!importado&&<section className="panel" style={{marginBottom:18}}><div className="panelHead"><div><h2>Base financeira ainda não carregada</h2><p>Importe sua base para visualizar os indicadores. Os dados ficam somente neste navegador.</p></div><button className="primary" onClick={()=>inputRef.current?.click()}><Upload size={16}/>Importar Excel/CSV</button></div></section>}<section className="cards controlCards"><article><div className="cardIcon income"><ArrowDownLeft size={19}/></div><div><span>Receita Familiar · 2026</span><strong>{moeda(anual.renda)}</strong><small>Salários e recebíveis</small></div></article><article><div className="cardIcon expense"><ArrowUpRight size={19}/></div><div><span>Despesas · 2026</span><strong>{moeda(anual.despesas)}</strong><small>Despesas Totais</small></div></article><article><div className="cardIcon balance"><ArrowDownLeft size={19}/></div><div><span>Fluxo de Caixa · 2026</span><strong>{moeda(anual.fluxo)}</strong><small>Fluxo de Caixa do Período</small></div></article></section><section className="infoGrid"><article className="panel chartPanel"><div className="panelHead"><div><h2>Receita x Despesas</h2><p>Comparação mensal de 2026</p></div></div><BarChart receita={receitaMensal} despesas={despesasMensal}/></article><article className="panel chartPanel"><div className="panelHead"><div><h2>Composição da Receita</h2><p>Salários e recebíveis</p></div></div><Donut values={receitaRows.map(r=>receitaMensal.reduce((s,_,i)=>s+valor(r.key,i),0))} total={anual.renda} labels={receitaRows.map(r=>r.label)}/></article><article className="panel chartPanel"><div className="panelHead"><div><h2>Composição das Despesas</h2><p>Despesas Totais</p></div></div><Donut values={despesaRows.map(r=>despesasMensal.reduce((s,_,i)=>s+valor(r.key,i),0))} total={anual.despesas} labels={despesaRows.map(r=>r.label)}/></article></section><section className="panel chartPanel wideChart"><div className="panelHead"><div><h2>Evolução do Fluxo de Caixa do Período</h2><p>Resultado mensal oficial da base importada</p></div><strong className="chartHighlight">{moeda(anual.fluxo)} no ano</strong></div><LineChart values={fluxoMensal}/></section><section className="panel monthlyPanel"><div className="panelHead"><div><h2>Indicadores financeiros</h2><p>Meses sempre na horizontal para comparação direta.</p></div><div className="savingBadge">Taxa anual de poupança: <b>{taxaPoupanca.toFixed(1)}%</b></div></div><div className="overflow-x-auto rounded-xl border border-slate-200 bg-white"><table className="min-w-[1450px] w-full border-collapse text-sm"><thead><tr className="bg-slate-100"><th className="sticky left-0 z-20 min-w-[260px] border-b border-r border-slate-200 bg-slate-100 px-4 py-3 text-left font-semibold">Indicador</th>{meses.map(m=><th key={m} className="min-w-[105px] border-b border-slate-200 px-3 py-3 text-right font-semibold">{m}</th>)}</tr></thead><tbody><tr><td className="sticky left-0 z-10 border-r border-t-2 border-teal-200 bg-teal-50 px-4 py-3 font-bold text-teal-900"><div className="flex min-w-[250px] items-center gap-2"><span>Salários e recebíveis</span><small className="rounded-full bg-white px-2 py-1 text-[9px] font-semibold text-teal-700">SUBTOTAL</small></div></td>{meses.map((m,i)=><td key={`renda-${m}`} className="border-t-2 border-teal-200 bg-teal-50 px-3 py-3 text-right font-bold text-teal-900 tabular-nums">{moeda(receitaMensal[i])}</td>)}</tr>{receitaRows.map(r=>linhaEditavel(r.label,r.key))}<tr><td className="sticky left-0 z-10 border-r border-t-2 border-sky-200 bg-sky-50 px-4 py-3 font-bold text-sky-900"><div className="flex min-w-[250px] items-center gap-2"><span>Despesas Totais</span><small className="rounded-full bg-white px-2 py-1 text-[9px] font-semibold text-sky-700">SUBTOTAL</small></div></td>{meses.map((m,i)=><td key={`desp-total-${m}`} className="border-t-2 border-sky-200 bg-sky-50 px-3 py-3 text-right font-bold text-sky-900 tabular-nums">{moeda(despesasMensal[i])}</td>)}</tr>{despesaRows.map(r=>linhaEditavel(r.label,r.key))}{fluxoRows.map(r=>linhaEditavel(r.label,r.key,r.key==='Fluxo de Caixa do Período'))}<tr><td className="sticky left-0 z-10 border-r border-t border-slate-200 bg-white px-4 py-3 font-semibold text-slate-700">Comprometimento da Renda</td>{meses.map((m,i)=>{const renda=receitaMensal[i],desp=despesasMensal[i],v=renda>0?desp/renda*100:0;return <td key={`comp-${m}`} className="border-t border-slate-200 px-3 py-3 text-right tabular-nums">{v?`${v.toFixed(1)}%`:'—'}</td>})}</tr></tbody></table></div></section><section className="panel"><div className="panelHead"><div><h2>Análise detalhada</h2><p>Conta, fornecedor, categoria e lançamento ficam na segunda tela.</p></div><Link className="primary" href="/lancamentos">Abrir lançamentos →</Link></div></section></main></div>}
